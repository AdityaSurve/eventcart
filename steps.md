# EventCart — Next Steps

**Done:** NestJS APIs, JWT httpOnly cookies, Google OAuth, WebAuthn passkeys, Redis, Kafka, React shop, admin analytics (OLS demand model), tests, Docker Compose.

---

## Optional follow-ups

1. Stricter order status state machine
2. `docker-compose.dev.yml` — Postgres + Redis + Kafka only; run `npm run start:dev` and `npm run dev` on the host
3. Google Cloud OAuth client IDs in `.env` when you want to try Google sign-in locally
