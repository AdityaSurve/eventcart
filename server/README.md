# EventCart API

See the [root README](../README.md) for full local setup.

```bash
npm install
npx prisma migrate dev
npx prisma db seed
npm run start:dev
```

- Swagger (development): `http://localhost:3000/api`
- Tests: `npm test` and `npm run test:e2e`
